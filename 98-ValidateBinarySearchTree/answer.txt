class Solution {
C++Auto13141516171819202122public:    bool util(TreeNode* root, long mini, long maxi) {
	if(!root)return true;
	if(root->val >=maxi or root->val<=mini)return false;
	return util(root->left, mini, root->val)&&util(root->right, root->val, maxi);
	
}
bool isValidBST(TreeNode* root)  {
	return util(root, LONG_MIN, LONG_MAX);
	
}

}
;
SavedLn 1, Col 1
}